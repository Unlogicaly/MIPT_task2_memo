#ifndef GAME_PATHS_H
#define GAME_PATHS_H

#include "iostream"
#include "split.h"

const std::string find_path();

const std::string game = find_path();
const std::string source = game + "source\\";

#endif // GAME_PATHS_H
